<div class="max-w-md mx-auto px-4 py-10">
  <h1 class="text-2xl font-black text-stone-800 text-center mb-8">🎯 目標を設定・編集</h1>
  
  <div class="bg-white p-8 rounded-[2.5rem] shadow-sm border border-stone-100">
    <%= form_with(model: @goal, local: true, class: "space-y-6") do |f| %>
      
      <% if @goal.errors.any? %>
        <div class="bg-red-50 text-red-500 p-4 rounded-xl text-sm font-bold">
          <ul>
            <% @goal.errors.full_messages.each do |message| %>
              <li>・<%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="space-y-4">
        <div>
          <%= f.label :target_amount, "今月の目標節約額", class: "block text-sm font-bold text-stone-600 mb-2" %>
          <div class="relative">
            <span class="absolute inset-y-0 left-0 pl-4 flex items-center text-stone-400 font-bold">¥</span>
            <%= f.number_field :target_amount, class: "block w-full pl-10 pr-4 py-3 bg-stone-100 border-none rounded-2xl focus:ring-2 focus:ring-orange-500 font-bold text-stone-700" %>
          </div>
          <p class="mt-1 text-xs text-stone-400">例: 自炊をして浮かせたい金額
          <br>↓モデルケース（1食あたり約250円節約として）
          <br>[仕事日のランチだけお弁当自炊（250円x22回=5500円）]
          <br>[毎日夜ご飯自炊（250円x31回=7750円）]
          </p>
        </div>

        <div>
          <%= f.label :target_times, "今月の目標自炊回数", class: "block text-sm font-bold text-stone-600 mb-2" %>
          <div class="relative">
            <%= f.number_field :target_times, class: "block w-full px-4 py-3 bg-stone-100 border-none rounded-2xl focus:ring-2 focus:ring-orange-500 font-bold text-stone-700" %>
            <span class="absolute inset-y-0 right-0 pr-4 flex items-center text-stone-400 font-bold">回</span>
          </div>
          <p class="mt-1 text-xs text-stone-400">例: 週5回ペースなら20〜22回</p>
        </div>
      </div>

      <div class="pt-4">
        <%= f.submit "目標を保存する", class: "w-full py-4 bg-orange-500 text-white font-bold rounded-2xl shadow-lg hover:bg-orange-600 transition active:scale-95" %>
      </div>
    <% end %>
  </div>
</div>