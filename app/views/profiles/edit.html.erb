<div class="max-w-md mx-auto mt-10 p-6 bg-white rounded-xl shadow-md">
  <h2 class="text-2xl font-bold mb-6 text-gray-800">プロフィール編集</h2>

  <%= form_with model: @user, url: profile_path, method: :patch, local: true do |f| %>
    
    <div class="mb-6">
      <%= f.label :name, "名前（ニックネーム）※公開レシピ時公開されます", class: "block text-sm font-medium text-gray-700 mb-2" %>
      <%= f.text_field :name, class: "w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-pink-500 focus:border-pink-500" %>
    </div>

    <%# アイコン画像の選択 %>
    <div class="mb-6">
      <%= f.label :avatar, "プロフィール画像 ※公開レシピ時公開されます", class: "block text-sm font-medium text-gray-700 mb-2" %>
      <div class="flex items-center gap-4">
        <%# 現在の画像（または👤）を表示 %>
        <div class="w-16 h-16 rounded-full overflow-hidden bg-gray-100 flex items-center justify-center border">
          <% if @user.avatar.attached? %>
            <%= image_tag @user.avatar, class: "w-full h-full object-cover" %>
          <% else %>
            <span class="text-2xl">👤</span>
          <% end %>
        </div>
        <%= f.file_field :avatar, class: "text-sm text-gray-500 file:mr-4 file:py-2 file:px-4 file:rounded-full file:border-0 file:text-sm file:font-semibold file:bg-pink-50 file:text-pink-700 hover:file:bg-pink-100" %>
      </div>
    </div>

    <div class="flex justify-end gap-3 mt-8">
      <%= link_to "キャンセル", profile_path, class: "px-4 py-2 text-sm font-medium text-gray-600 hover:text-gray-800 transition" %>
      <%= f.submit "変更を保存する", class: "px-4 py-2 bg-pink-500 text-white rounded-lg font-bold hover:bg-pink-600 transition shadow-md cursor-pointer" %>
    </div>

  <% end %>
</div>