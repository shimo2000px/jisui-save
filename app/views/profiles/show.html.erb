<div class="max-w-3xl mx-auto py-10 px-4">
  <div class="bg-white shadow rounded-2xl overflow-hidden">
    <div class="h-32 bg-gradient-to-r from-orange-400 to-pink-400"></div>

    <div class="relative px-6 pb-6">

      <div class="relative -top-12 flex justify-between items-end">
        <div class="p-1 bg-white rounded-full">
          <% if @user.avatar.attached? %>
            <%= image_tag @user.display_avatar, class: "w-32 h-32 rounded-full object-cover border-4 border-white shadow-sm" %>
          <% else %>
            <div class="w-32 h-32 rounded-full bg-gray-200 flex items-center justify-center text-4xl border-4 border-white shadow-sm">👤</div>
          <% end %>
        </div>
        <%= link_to "編集する", edit_profile_path, class: "mb-2 bg-white border border-gray-300 px-4 py-2 rounded-lg text-sm font-bold hover:bg-gray-50 transition shadow-sm" %>
      </div>

      <%# ユーザー情報 %>
      <div class="-mt-8">
        <h1 class="text-2xl font-bold text-gray-900"><%= @user.name %></h1>
        <p class="text-gray-500 text-sm"><%= @user.email %></p>
      </div>

      <hr class="my-6 border-gray-100">

      <%# 今後実装予定の統計情報エリア %>
      <div class="grid grid-cols-2 gap-4 mb-8">
        <div class="bg-orange-50 p-4 rounded-xl text-center">
          <p class="text-gray-600 text-xs uppercase font-bold tracking-wider">今月の節約額</p>
          <p class="text-2xl font-black text-orange-600">¥0 <span class="text-sm font-normal text-gray-400">(予定)</span></p>
        </div>
        <div class="bg-emerald-50 p-4 rounded-xl text-center">
          <p class="text-gray-600 text-xs uppercase font-bold tracking-wider">自炊回数</p>
          <p class="text-2xl font-black text-emerald-600">0 <span class="text-sm font-normal text-gray-400">回</span></p>
        </div>
      </div>

      <%# 今後実装予定のグラフ、Chart.js 用のエリア %>
      <div class="bg-gray-50 border-2 border-dashed border-gray-200 rounded-2xl p-10 flex flex-col items-center justify-center text-gray-400">
        <span class="text-4xl mb-2">📊</span>
        <p class="text-sm font-medium">